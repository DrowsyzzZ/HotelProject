# 3일 개발 계획

## Day 1 - 프로젝트 기반과 메인

### #1 프로젝트 및 GitHub 초기 설정

- Git 저장소와 `.gitignore` 확인
- README, 이슈 템플릿, PR 템플릿 구성
- JSON Server 실행 확인
- Swiper 적용 방식 결정

완료 조건:

- 저장소를 새로 clone한 뒤 설치 및 실행할 수 있다.
- `node_modules`와 편집기 설정이 커밋되지 않는다.

### #2 공통 레이아웃 구현

- CSS Reset과 디자인 변수
- 공통 콘텐츠 컨테이너
- `<app-header>` Custom Element와 내비게이션
- `<app-footer>` Custom Element
- `<top-button>` Custom Element와 스크롤 이벤트

완료 조건:

- 모든 페이지에서 공통 레이아웃을 재사용할 수 있다.
- PC와 모바일에서 가로 스크롤이 발생하지 않는다.
- 세 공통 요소가 각자의 Custom Element 파일로 분리되어 있다.

### #3 메인 페이지 UI 구현

- 메인 비주얼
- ROOMS, EVENT, BEST HOTEL 섹션
- PDF 기준 PC/모바일 구조

완료 조건:

- 메인 페이지의 전체 섹션과 콘텐츠가 표시된다.

### #4 메인 Swiper 구현

- 메인 이미지 슬라이더
- 객실·이벤트 슬라이더
- 페이지네이션과 터치 동작

완료 조건:

- PC와 모바일에서 슬라이드가 정상 작동한다.
- 콘솔 오류가 없다.

## Day 2 - 예약 기능

### #5 예약 안내 페이지 구현

- 객실 가격표
- 숙박·식사·세부 사항
- 취소 및 환불 규정
- `rooms`, `season`, `price` 데이터 연동

완료 조건:

- `db.json`을 기준으로 객실별 가격이 표시된다.

### #6 객실 선택 및 상세 구현

- `<room-card>` Custom Element를 이용한 객실 4종 동적 렌더링
- `roomId` URL 파라미터 전달
- 객실 설명과 `<room-gallery>` Custom Element

완료 조건:

- 선택한 객실의 데이터가 상세 화면에 표시된다.
- 객실 데이터 객체를 Custom Element의 프로퍼티로 전달한다.

### #7 예약 달력 구현

- `<reservation-calendar>` Custom Element 구현
- 월 이동과 날짜 렌더링
- 과거·예약 완료 날짜 비활성화
- 체크인·체크아웃 범위 선택
- 기존 예약과 중복되는 범위 차단
- `date-range-change` Custom Event로 선택 결과 전달

완료 조건:

- 유효한 숙박 범위만 선택할 수 있다.
- 달력의 상태와 이벤트 처리가 페이지 스크립트에서 분리되어 있다.

### #8 인원 및 가격 계산 구현

- 기본·최대 인원 검증
- 추가 인원 금액
- 성수기·비수기 판별
- 평일·주말·공휴일 가격
- 총합계 계산

완료 조건:

- 날짜나 인원이 바뀌면 총금액이 즉시 다시 계산된다.

### #9 예약 폼과 등록 구현

- 선택 데이터 `sessionStorage` 전달
- 예약자명과 전화번호 검증
- 등록 직전 중복 예약 재검사
- `POST /reservation`

완료 조건:

- 올바른 예약만 저장되고 중복 제출이 방지된다.

## Day 3 - 반응형과 검수

### #10 전체 모바일 반응형 적용

- 메인, 예약 안내, 객실 선택
- 상세·달력·예약 폼
- 360px 모바일 화면 검수

완료 조건:

- PDF 모바일 화면과 동일한 콘텐츠 순서를 유지한다.
- 잘림과 불필요한 가로 스크롤이 없다.

### #11 예외 및 오류 처리

- API 연결 실패
- 빈 데이터
- 잘못된 객실 ID
- 입력 오류 및 중복 제출

완료 조건:

- 주요 실패 상황에 사용자 안내가 표시된다.

### #12 통합 테스트 및 버그 수정

- 메인부터 예약 등록까지 전체 흐름
- 객실별 인원 제한
- 성수기·비수기·주말·공휴일 계산
- 예약 중복 차단
- PC·모바일 검수

완료 조건:

- 핵심 사용자 흐름을 중단 없이 완료할 수 있다.
- 콘솔 오류가 없다.

## 브랜치 운영

- `feat/base-home`: #1-#4
- `feat/reservation`: #5-#9
- `feat/responsive-qa`: #10-#12

이슈 하나가 끝날 때마다 커밋하고, 큰 브랜치가 끝나면 `main`에 병합합니다.

## Custom Element 적용 범위

```text
필수
├─ app-header
├─ app-footer
└─ top-button

추가
├─ room-card
├─ room-gallery
└─ reservation-calendar
```

일반 버튼, 입력 필드, 메뉴 항목, 제목, 가격표 행과 셀은 Custom Element로 만들지 않습니다. 네이티브 HTML의 접근성과 기본 동작을 유지하고 CSS 클래스 또는 JavaScript 렌더 함수로 재사용합니다. Custom Element에는 Shadow DOM을 적용하지 않습니다.
